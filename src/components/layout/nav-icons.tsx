import {
  BookOpen,
  FlaskConical,
  Globe,
  GraduationCap,
  User,
  Users,
  type LucideIcon,
} from "lucide-react";

// Maps the string icon names used in config/navigation.ts to lucide components.
export const NAV_ICONS: Record<string, LucideIcon> = {
  User,
  FlaskConical,
  BookOpen,
  GraduationCap,
  Users,
  Globe,
};
